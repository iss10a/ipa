# Single source of truth for the app identity.
#
# No team id, provisioning profile or certificate belongs in this file or
# anywhere else in the repository. Signing happens after the IPA is built.
#
# Change this value, then run:  bash tool/apply_ios_config.sh

APP_BUNDLE_ID = com.iss10a.downloader
